import React from "react";

// Placeholder for HotTable integration
export default function HotTableWrapper(props: any) {
  return (
    <div style={{ border: '1px solid #eee', padding: 16, borderRadius: 8, background: '#fafafa' }}>
      <p>HotTable component placeholder</p>
      {props.children}
    </div>
  );
}
