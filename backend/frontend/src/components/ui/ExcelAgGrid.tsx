import React from "react";

// Placeholder for ExcelAgGrid integration
export default function ExcelAgGrid(props: any) {
  return (
    <div style={{ border: '1px solid #eee', padding: 16, borderRadius: 8, background: '#fafafa' }}>
      <p>ExcelAgGrid component placeholder</p>
      {props.children}
    </div>
  );
}
